{% extends 'base.html.twig' %}

{% block title %}Scolarite{% endblock %}

{% block body %}
    <h1>Scolarite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEtab</th>
                <td>{{ scolarite.idEtab }}</td>
            </tr>
            <tr>
                <th>NomEtablissement</th>
                <td>{{ scolarite.nomEtablissement }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ scolarite.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ scolarite.pays }}</td>
            </tr>
            <tr>
                <th>Diplome</th>
                <td>{{ scolarite.diplome }}</td>
            </tr>
            <tr>
                <th>DateObtention</th>
                <td>{{ scolarite.dateObtention ? scolarite.dateObtention|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scolarite_index') }}">back to list</a>

    <a href="{{ path('app_scolarite_edit', {'idEtab': scolarite.idEtab}) }}">edit</a>

    {{ include('scolarite/_delete_form.html.twig') }}
{% endblock %}
