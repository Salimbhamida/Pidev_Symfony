<form method="post" action="{{ path('app_scolarite_delete', {'idEtab': scolarite.idEtab}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scolarite.idEtab) }}">
    <button class="btn">Delete</button>
</form>
