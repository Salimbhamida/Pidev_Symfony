<form method="post" action="{{ path('app_experiences_delete', {'idExp': experience.idExp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience.idExp) }}">
    <button class="btn">Delete</button>
</form>
