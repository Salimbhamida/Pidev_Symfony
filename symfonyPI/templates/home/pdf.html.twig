<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{ title }}</title>
    </head>
    <body>
        <h1>{{ title }}</h1>
        <p>{{ content }}</p>
        {% for d in demandes %}
             <p> {{d.experience}} </p>
             <p> {{d.description}} </p>
             <p> {{d.remuneration}} </p>
        {% endfor %}
    </body>
</html>